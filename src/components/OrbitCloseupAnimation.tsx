// import useLottie from '../hooks/useLottie';


// interface OrbitCloseupAnimationProps {
//     id: string;
//     player: React.ElementType;
//     desc: string;
//     activeId: string;
// }

// const OrbitCloseupAnimation: React.FC<OrbitCloseupAnimationProps> = ({id, player, desc}) => {
//     const {animation} = useLottie(id + desc)
//     const Player = player

//     return (
//         <div>
//             <Player src={animation} className={`${orbitLineContainer} ${id === activeId ? 'drawing' : ''}`} autoplay/>
//         </div>
//     )
// }

// export default OrbitCloseupAnimation